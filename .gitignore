# dependências e build
node_modules/
.next/
out/

# variáveis de ambiente (chaves do Supabase etc.)
.env
.env*.local

# configurações locais do Claude Code
.claude/settings.local.json

# diversos
.DS_Store
*.log

tsconfig.tsbuildinfo

# Vercel
.vercel
.env*
!.env.example

# projetos paralelos na mesma pasta (não fazem parte do app)
n8n/

# backups do banco de dados (dados pessoais, nunca versionar)
backups/

# skills instaladas por `npx skills add` — codigo de terceiros, como node_modules.
# O skills-lock.json fica versionado para dar pra reinstalar identico.
.agents/
.claude/skills/
